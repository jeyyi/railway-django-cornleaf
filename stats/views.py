from django.shortcuts import render
from django.db import connections

# Create your views here.
def get_statistics(date):
    return 'ardy'


def get_active_db_connections(request):
    # Get the default database connection
    return ''
